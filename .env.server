DATABASE_URL="postgresql://postgres:postgres@localhost:5432/thesis_grey"

# Add other server-only environment variables below if needed (e.g., API keys)
# JWT_SECRET=replace-with-a-strong-secret
# SERPER_API_KEY=replace-with-your-serper-key
